:443 {
	root * /srv
	file_server
	encode gzip

	# Handle SPA routing - serve index.html for non-existent paths
	try_files {path} /index.html
}

junos.dk {
	tls internal

	reverse_proxy http://junos.dk:80
}